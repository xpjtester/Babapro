const c=document.getElementById('wreck'),x=c.getContext('2d'),scoreEl=document.getElementById('score');let W,H,keys={},craneX=.22,angle=.55,av=0,score=0,blocks=[],ball={},last=0,started=false;function size(){let r=c.getBoundingClientRect();c.width=r.width*devicePixelRatio;c.height=r.height*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);W=r.width;H=r.height;reset()}addEventListener('resize',size);function reset(){score=0;scoreEl.textContent='€0 DAMAGE';craneX=.22;angle=.55;av=0;started=false;blocks=[];for(let row=0;row<6;row++)for(let col=0;col<5;col++)blocks.push({x:W*(.56+col*.055),y:H*(.66-row*.075),w:W*.05,h:H*.07,alive:true,value:32000});for(let row=0;row<3;row++)for(let col=0;col<2;col++)blocks.push({x:W*(.86+col*.05),y:H*(.66-row*.075),w:W*.045,h:H*.07,alive:true,value:-250000,red:true})}addEventListener('keydown',e=>{keys[e.code]=true;if(['ArrowLeft','ArrowRight','Space'].includes(e.code))e.preventDefault()});addEventListener('keyup',e=>{keys[e.code]=false;if(e.code==='Space')started=true});document.getElementById('reset').onclick=reset;function update(dt){if(keys.ArrowLeft)craneX=Math.max(.12,craneX-dt*.18);if(keys.ArrowRight)craneX=Math.min(.43,craneX+dt*.18);if(keys.Space){av+=dt*1.8;angle+=av;started=true}else if(started){av+=Math.sin(angle)*1.35*dt;av*=.997;angle+=av}let p={x:craneX*W,y:.16*H},len=.42*H;ball={x:p.x+Math.sin(angle)*len,y:p.y+Math.cos(angle)*len,r:Math.max(18,W*.025)};blocks.forEach(b=>{if(!b.alive)return;let cx=Math.max(b.x,Math.min(ball.x,b.x+b.w)),cy=Math.max(b.y,Math.min(ball.y,b.y+b.h));if(Math.hypot(ball.x-cx,ball.y-cy)<ball.r&&Math.abs(av)>.22){b.alive=false;score+=b.value;scoreEl.textContent=(score<0?'-':'')+'€'+Math.abs(score).toLocaleString()+' DAMAGE'}})}function draw(){x.clearRect(0,0,W,H);let g=x.createLinearGradient(0,0,0,H);g.addColorStop(0,'#cbd4dd');g.addColorStop(1,'#eee6d8');x.fillStyle=g;x.fillRect(0,0,W,H);x.fillStyle='#b6a58e';x.fillRect(0,H*.73,W,H*.27);x.fillStyle='#d1a52c';x.fillRect(craneX*W-W*.09,H*.57,W*.18,H*.11);x.fillStyle='#333';x.fillRect(craneX*W-W*.11,H*.66,W*.22,H*.045);x.strokeStyle='#c89f28';x.lineWidth=12;x.beginPath();x.moveTo(craneX*W,H*.58);x.lineTo(craneX*W,H*.12);x.stroke();x.lineWidth=8;x.beginPath();x.moveTo(craneX*W,H*.15);x.lineTo(craneX*W+W*.25,H*.08);x.stroke();let p={x:craneX*W,y:.16*H};x.strokeStyle='#772b28';x.lineWidth=5;x.beginPath();x.moveTo(p.x,p.y);x.lineTo(ball.x,ball.y);x.stroke();x.fillStyle='#222';x.beginPath();x.arc(ball.x,ball.y,ball.r,0,7);x.fill();blocks.forEach(b=>{if(!b.alive)return;x.fillStyle=b.red?'#b84a42':'#aaa7a0';x.fillRect(b.x,b.y,b.w,b.h);x.strokeStyle='#666';x.strokeRect(b.x,b.y,b.w,b.h)});x.fillStyle='#9b332d';x.font='bold 11px Verdana';x.fillText('PROTECTED',W*.85,H*.4)}function loop(t){let dt=Math.min(.025,(t-(last||t))/1000);last=t;update(dt);draw();requestAnimationFrame(loop)}size();requestAnimationFrame(loop);